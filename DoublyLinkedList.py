'''
Implement a doubly linked list data
structure with push, pop, insert, and
traverse methods
'''

# In a LIFO stack, if an element is inserted in the middle,
# is that the element that would be popped since it was the
# last one in?

class DoublyLinkedList:

	def __init__(self):

		self.root = None
		self.next = None
		self.prev = None

	def push(self, s):

		self.next = s



	def pop(self):


	def insert(self):

